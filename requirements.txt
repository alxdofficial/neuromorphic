# Core dependencies for neuromorphic LM training

# PyTorch (install separately with CUDA support for your system)
# See: https://pytorch.org/get-started/locally/
torch>=2.0.0

# HuggingFace libraries
# transformers >=5.0 required for the modern DynamicCache API
# (cache.layers[i].keys/.values/.is_initialized/.get_seq_length()) used
# by src/trajectory_memory/tbptt.py:_trim_kv_cache + _detach_kv_cache.
# Older 4.x cache was tuple/list-shaped — incompatible.
transformers>=5.0.0
datasets>=2.14.0
tokenizers>=0.15.0

# Progress bars
tqdm>=4.65.0

# Numerical computing
numpy>=1.24.0

# Data pipeline
pyarrow>=12.0.0
